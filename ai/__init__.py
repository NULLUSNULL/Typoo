# ai/
# Integración opcional con asistentes de IA (nube, local y —más adelante—
# modelos embebidos). Todo el subsistema es opt-in: si la IA no está habilitada
# en la configuración, nada de esto se utiliza.
